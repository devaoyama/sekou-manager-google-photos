import React from "react";
import Auth from "../components/Auth";

const Index = () => {
    return (
        <Auth>
            <div>Hello React</div>
        </Auth>
    );
};

export default Index;
